// UsersController endpoints removed — disabled per request.
// If you need to restore them later, re-add the controller methods and the
// `@Controller('users')` decorator.
export class UsersController {}
